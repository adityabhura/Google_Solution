<%- include("partials/header") %>
<div class="products">
    <div class="ui four column doubling stackable grid container">
        <% products.forEach(function(product){ %>
        <div class="column">
            <div class="ui link cards">
                <div class ="card">
                    <div class="image">
                        <img src="<%= product.image[0] %>">
                    </div>  
                    <div class="content">
                        <div class="header"><%= product.title %></div>
                        <div class="meta">By <%= product.bookauthor%></div>
                    </div>
                    <div class="extra content">
                        <a href="/products/<%= product._id %>" class="right floated"><button class="ui inverted primary button">Show more</button></a>
                        <div class="header">Rs <%= product.amount %> /week</div>
                    </div> 
                    
                    <!-- <% if(!currentUser){ %>
                        <div class="extra content">
                            <form action="/bookmark/notAuthorised" method="POST">
                                <input type="submit" name="bookmark" value="YES">
                            </form>
                        </div> 
                    <% } %>
                    <div class="extra content">
                        <form action="/products" method="POST">
                            <input type="submit" name="bookmark" value="YES">
                        </form>
                    </div>          -->
                    

                </div>
            </div>
        </div>
            <% }); %>       
    </div>
</div>
<%- include("partials/footer") %>
