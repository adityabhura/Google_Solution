<%- include("partials/header") %>

<div class="products">
    <h1><%= user%></h1>
    <h1><%= user.username %></h1>
</div>
<% if(currentUser && user._id.equals(currentUser._id)){ %>
    <div>
        <button><a href="/profiles/<%= currentUser._id %>/mybookmarks">My Bookmarks</a></button>
        <button><a href="/profiles/<%= currentUser._id %>/myproducts">Uploaded Books</a></button>
    </div
<% } %>


<%- include("partials/footer") %>